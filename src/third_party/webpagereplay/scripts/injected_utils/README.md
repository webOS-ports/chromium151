# Injected Utilities

This directory is intended for utilities that are not strictly part of WebPageReplay (WPR), but that users might find useful after some adjustment.

For example, `dismisser.js` can be modified to dismiss cookie banners at the start of a replay.
